// Previously a disposable PostgreSQL catalog integration test.
// Skipped after MariaDB migration — rewrite against MariaDB before re-enabling.
console.error(
  'catalog-offers.integration.mjs is disabled until rewritten for MariaDB.',
);
process.exit(0);
