# Database migrations

Old PostgreSQL migrations were replaced by this MariaDB baseline (`1790000000000-InitMariaDbSchema.ts`).

A **fresh MariaDB database** is required. Do not run this migration against an existing PostgreSQL database or expect to migrate live PG data in place.
